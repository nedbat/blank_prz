A blank presentation to use as a starting point for new talks.


git archive --format=tar --prefix=newprezname/ @ | (cd .. && tar xf -)

Then in newprezname, rename blank.html, and edit Makefile to change the SLUG.
